# data/

Dataset lives under `data/dataset/`.

If you want to use your own recordings:
1) Replace the contents of `data/dataset/data/` with your WAV folders
2) Update `data/dataset/labels.csv`

See `data/dataset/README.md` for the exact schema.
